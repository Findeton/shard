# Order owns the conformal direction, the count owns the volume up to one scale, and manifoldlikeness is the gate: the continuum companion to the gravity no-go

**Author:** Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)

**Status:** preprint, not peer reviewed, companion to publishable Paper XI, version 2026-06-15.

## Abstract

Paper XI proved that the sealed-records program derives gravity's equation of state but cannot fix its absolute scale: the records carry only weight-zero (relative) invariants, while Newton's `G` — equivalently the seal-spacing length `l_step` — is the one absolute datum no record functional can be. This companion asks the same question on the *continuum* side, for the structure under that no-go: can the Lorentzian metric itself be built from the records? The records own exactly two pre-geometric data — a Lorentz-invariant **causal order** of seals and a **seal count** — which is precisely the input the causal-set programme's founding identity *order + number = geometry* assembles into a metric. We price that assembly in three honestly separated tiers. **(I)** The pure causal order recovers the **conformal structure** — null cones, dimension, and scale-free **directions** — with no length input (Malament–HKMM; Myrheim–Meyer): demonstrated on the record order, dimension `1+1 → 2.01`, `2+1 → 3.01`, the order and its null skeleton bit-identical under boosts and global dilations. **(II)** The seal count is an unbiased Poisson **volume counter** (`E[N] = V/l_step^d`), recovering the volume *measure* but only in units of `l_step^d`: the absolute volume is weight `+d`, walled; ratios are weight `0`, owned to `1.5%`. **(III)** Tiers I–II are theorems **only for manifold-like causal sets**, and non-manifold orders dominate (Kleitman–Rothschild `2^{n²/4}`) and fool a single dimension estimator.

**What is external, and what is the program-internal contribution, stated up front.** *Every theorem and every number reproduced by the receipts is standard causal-set theory* — order-determines-conformal (Malament–HKMM), the Myrheim–Meyer dimension estimator, order + number = geometry (Bombelli–Lee–Meyer–Sorkin), and the Kleitman–Rothschild dominance of 3-layer orders. We claim no new mainstream theorem. The contribution is a *positioning statement about the records*: (a) the conformal **direction** is order-owned and scale-free, so Paper XI's continuum-side missing input — a propagation direction for an emergent-metric structure — is *order-determined*, **not** an instance of the absolute-scale wall; (b) the volume axis is one more recurrence of Paper XI's *same* `l_step` no-go, closed by inputting `G`; and (c) **manifoldlikeness** is named as the field-shared gate the whole sealed-record gravity line presupposes. These are a positioning, a no-go recurrence, and a named gate — statements about what the records do and do not own — not theorems of causal-set theory.

## 1. What this companion adds to Paper XI

Paper XI gave the sealed-record gravity sector its two-sided verdict on the *equation of state*: the records derive the Einstein form (temperature + pure-area Wald entropy + modular/Clausius first law) and prove they cannot fix the absolute scale `σ_A` ↔ `G` ↔ `l_step` (the weight-counting no-go, made concrete by SIGMA-SPLIT). That verdict lives on the thermodynamic side. It leaves a continuum-side residue untouched: the *metric* itself — the Lorentzian geometry whose causal order and volume the thermodynamics presumes. This companion prices that residue with the one tool built for it, the causal-set identity *order + number = geometry*, and shows the residue splits cleanly into a part the order owns (the conformal direction), a part walled by the very same scale wall as Paper XI (the volume), and a gate the whole construction stands on (manifoldlikeness).

The discipline is pre-geometric in a careful sense, and the honesty mandate of this batch requires it be said plainly. The receipts build a causal order from a coordinate sprinkling **only to test** whether an order-only reconstruction works; every reconstructed quantity is computed from the order and the count *alone*, with the coordinates held out purely as ground truth to *score* the reconstruction, never fed to the estimator. What such a test can establish is a *conditional*: **if** the record order is manifold-like, order + number recovers the conformal structure and the volume-up-to-`l_step`. What it cannot establish — and §4 makes this the controlling limitation — is that the record dynamics *produces* manifold-like orders. That is the field's open core, not ours to discharge.

## 2. Order → the conformal direction, scale-free [external theorem; program positioning]

**External (standard causal-set theory).** The conformal half of the metric is owned by the pure order, by two textbook results. The **Malament–HKMM theorem** (Hawking–King–McCarthy 1976; Malament 1977): for a manifold-like, past- and future-distinguishing spacetime, the causal order determines the metric *up to a local conformal factor* — i.e. the null cones and every direction, everything scale-free. Its discrete companion is the **Myrheim–Meyer** dimension estimator: the *ordering fraction* (the fraction of pairs causally related) is a function of the dimension alone, `f(d) = Γ(d+1)Γ(d/2) / [2Γ(3d/2)]`, giving the exact `f(2) = 1/2`, `f(3) = 8/35`, `f(4) = 1/10`, with no length scale in it. Receipt `r1` reproduces these on the record order (9/9 checks); none of this is new mathematics.

What the receipt exhibits, all from the *pure order*, with coordinates held out only to score:

- **Dimension from the order.** A `1+1` Alexandrov order has ordering fraction `0.4963` against the exact `f(2) = 1/2`, giving Myrheim–Meyer dimension `2.01`; a `2+1` order gives `0.2259` against `f(3) = 8/35 = 0.228571`, dimension `3.01` (`r1`). The cone-opening — a conformal datum — is read off the order sign alone, with no `s²` magnitude.
- **The null cone from the links.** The order's *links* (covering relations) concentrate on the null directions as density grows: the median link slope `|dx/dt|` rises toward the null value `1` (`0.83` at `N = 400`, `0.93` at `N = 3000`; `r1`).
- **Boost- and dilation-invariance.** A boost is an order automorphism (order matrix and link skeleton bit-identical), and a global dilation `x → λx` (`λ = 0.137, 7.3`) leaves the *entire order and every order-derived direction unchanged* (`r1`): the conformal/direction structure is scale-free, exactly as Malament's conformal class is.

**Program-internal contribution (positioning, not a theorem).** The unit propagation direction `n̂` in the emergent-metric structure, and the "premature / unbuilt" framing of it, are the *v7 graviton companion's* (Paper X, the spin-2 pricing that sits under Paper XI's scale no-go), not parent Paper XI's; Paper X correctly scopes `n̂` as scale-free, *not* the absolute-scale wall. The positioning here is to make that precise and *attribute* it: the order-only content is that the order and its link/null skeleton are bit-identical under any dilation, and Malament–HKMM says that conformal class *is* the directions. A representative link direction, scored against the held-out geometry purely to *check* this, is correspondingly dilation-invariant (drift `0.00` under `λ = 7.3`) while its *length* scales by `λ` (`2.70 → 19.70`; `r1`). So the propagation direction is an order-determined, scale-free member of the conformal class — **owned in principle by the record causal order, and provably not an instance of the `l_step` scale wall**. This reclassifies Paper X's continuum-side missing direction from "premature / unbuilt" to *order-determined, gated only by manifoldlikeness* (§4). The positioning is the contribution; the underlying order → conformal map is Malament's, the dimension estimator is Myrheim–Meyer's.

## 3. Number → the volume measure, up to `l_step` [the same no-go as Paper XI, on the volume axis]

**External.** The volume half is the seal **count**, an unbiased volume counter by the Myrheim–Meyer cardinality relation `E[N] = V / l_step^d`: the number of seals in a region estimates its volume in units of the fundamental volume element `l_step^d`. Receipt `r2` confirms it (6/6): the count is unbiased (`|⟨N⟩ − E[N]|/E[N] = 0.5%`) and Poisson (`Var[N]/E[N] = 0.97`), recovering the volume to `~1/√N`. This is standard sprinkling theory.

**Program-internal contribution (the same no-go as Paper XI).** The count recovers the volume *in `l_step^d` units*, and the absolute scale is walled — by exactly Paper XI's weight-counting mechanism, now on the volume axis. Under a relabelling `l_step → μ·l_step`, the **count is invariant** (a weight-0 integer, `49` whatever `μ`) while the **physical volume scales as `μ^d`** (`49 → 196` for `l_step = 1, 2`; `r2`). The weight ledger reads: volume-in-`l_step^d`-units (`= N`) is weight `0` and **owned**; absolute physical volume (`= N·l_step^d`) is weight `+d` and **walled**; volume *ratios* are weight `0` and owned exactly (count-ratio `0.355` against the true `0.36`, error `1.5%`; `r2`). This is *not* a new wall — it is Paper XI's `σ_A`/`G` no-go recurring on the conformal-factor / volume axis: the same weight-zero-only record substrate, the same absolute length it cannot supply. The one external input that closes it is the same one Paper XI names: **`G`**. In natural units `G ∼ l_step²`, so inputting the Newton constant fixes `l_step` and hence the absolute volume normalization — the continuum-side face of Paper XI's "Einstein equations up to `G`." The records own the volume *measure*; `G` is the one number that turns it into absolute volume. The contribution is the *identification* — that the metric's volume axis is the very wall Paper XI proved, closed by the very input Paper XI names — not the cardinality relation, which is Myrheim–Meyer's.

## 4. Manifoldlikeness — the gate the whole gravity line presupposes [external; named here]

**External (standard, and a field-wide open problem).** Tiers I–II are theorems **only for manifold-like causal sets** — those faithfully embeddable in a Lorentzian manifold. This is the hypothesis of Malament's theorem and the regime where the Myrheim–Meyer estimators are valid. A generic order is *not* manifold-like, and the obstruction is sharp. The **Kleitman–Rothschild theorem** (1975): the number of partial orders on `n` elements grows as `2^{n²/4 + o(n²)}`, almost all of them 3-layer, height-2 "KR" orders. Receipt `r3` makes the count concrete: at `n = 10⁴`, the dominant family carries `~2.5 × 10⁷` bits against `~5 × 10⁵` bits of manifold-like embedding data — the non-manifold orders overwhelm the manifold-like ones by the square of the size. And a single Myrheim–Meyer estimate is *fooled* by them: a genuine sprinkling recovers its embedding dimension (`2d → 1.99`, `3d → 3.00`), but a KR order returns a spurious near-integer `d_MM = 2.38`, unmasked only by a *second*, height-based estimator (KR longest chain `3` versus a `2d` sprinkling's `46`; `r3`, 17/17). All of this is established causal-set combinatorics.

**Program-internal contribution (naming the gate).** The contribution here is not a theorem but a *naming*: manifoldlikeness is the unstated floor under every order → conformal step in this corpus and under the sealed-record gravity line's continuum premises, and the corpus had never named it. Whether the record click-law dynamics *produces* manifold-like orders rather than the dominant KR junk is the causal-set field's central open problem — the entropy / dominance problem, shared by every causal-set and emergent-gravity programme. **SHARD inherits it; it does not solve it.** The first recorded symptom is the foundation paper's own "non-discriminating finite-sprinkling drift" — the record dynamics, run on a test order, did not discriminate the manifold-like structure; read here as a symptom of this gate. A further leg, the causal-set **Hauptvermutung** (that a causal set embeds into *at most one* manifold up to approximate isometry), is open in general, so order fixes a *unique* conformal manifold only modulo it. The honest split is the lasting point: `l_step` is **SHARD's own** wall — a proved scale impossibility (Paper XI), closed by *inputting* `G`; manifoldlikeness is the **field's** wall — an open emergence problem, not proved either way and not closeable by inputting any constant. Naming it, and locating it as the gate Tiers I–II and Paper XI's continuum premises stand on, is the program-internal statement.

## 5. What this assembles, and what it does not claim

Put the three tiers together and the continuum companion to Paper XI's no-go reads:

> **The emergent metric continuum is buildable up to the single absolute scale `l_step`, *given* manifoldlikeness.** The causal **order** delivers the conformal structure and the scale-free **directions** (Tier I); the seal **count** delivers the **volume measure up to `l_step`**, with `G` the input that closes the scale (Tier II); and the whole construction is **gated on manifoldlikeness** (Tier III).

**Claims (about the records, not new mathematics).** (1) The conformal direction is order-owned and scale-free, so Paper XI's continuum-side missing direction is *order-determined*, not the absolute-scale wall — a positioning that reduces the open surface, since the direction is now owned-in-principle rather than missing. (2) The metric's volume axis is one more recurrence of Paper XI's `l_step`/`G` no-go, closed by inputting `G`. (3) Manifoldlikeness is the field-shared gate the sealed-record gravity line presupposes, named here for the first time, with the click-law offering no proven lever on it.

**Non-claims.** This companion does **not** prove any causal-set theorem — Malament–HKMM, Myrheim–Meyer, order + number = geometry, and Kleitman–Rothschild dominance are entirely external and are invoked, not re-derived. It does **not** discharge manifoldlikeness — that is the field's open core, and the record dynamics is not shown to select manifold-like orders (the evidence to date is non-discrimination). It does **not** fix the scale — Paper XI's `l_step`/`G` no-go stands; `G` is an *input* that closes the volume normalization, not a derivation. It does **not** construct a *unique* emergent manifold (the Hauptvermutung leg is open). The coordinate sprinkling in the receipts is a test scaffold scored against held-out ground truth, never a derivation input.

## Reproducibility

All numeric claims are reproducible at mpmath `dps ≥ 60` or by exact combinatorics, and every number cited above carries a receipt in `code/`. Receipt `r1_order_to_conformal_direction.py` (9/9 checks pass) reproduces the order-only dimension recovery (`1+1 → 2.01`, `2+1 → 3.01`; exact `f(2) = 1/2`, `f(3) = 8/35`), the null-cone link concentration (median slope `0.83 → 0.93`), and the boost/dilation invariance of the order and its directions (direction drift `0.00`, length `2.70 → 19.70` under `λ = 7.3`). Receipt `r2_number_volume_lstep.py` (6/6) reproduces the volume counter (`E[N] = V/l_step^d`, bias `0.5%`, Poisson `Var/E = 0.97`), the `l_step` rescaling ledger (count invariant `49`; volume `49 → 196`), and the owned volume ratios (`1.5%`). Receipt `r3_manifoldlikeness_myrheim_meyer.py` (17/17) reproduces the Kleitman–Rothschild dominance (`n²/4` exponent; `~2.5 × 10⁷` vs `~5 × 10⁵` bits at `n = 10⁴`), the Myrheim–Meyer trap (`d_MM = 2.38` on a KR order), and the height-estimator unmasking (`3` vs `46`). All three receipts exit cleanly.

## References

**Companion (this program).**
- *Emergent Einstein equations without an emergent Newton constant: a no-go for sealed-record gravity* (publishable Paper XI) — the parent: gravity's equation of state derived, its absolute scale `σ_A` ↔ `G` ↔ `l_step` proved un-fixable (weight-counting no-go, SIGMA-SPLIT); the continuum-side missing direction and the volume axis this companion prices.
- Relativistic ISP v6 paper 1, *Indivisible Causal-Set Gravity* — the seal / division-event causal set, order + number = geometry, the hypersurface-deformation route to general relativity up to `G` and `Λ`, and the "non-discriminating finite-sprinkling drift" read here as the first symptom consistent with the manifoldlikeness gate.
- *The record click-law, III / IV / VII / X* (v7) — the `l_step` scale no-go (III); the entanglement graph and hop-metric up to scale (IV); the covariance result conditional on an emergent Lorentz action (VII); the graviton propagation direction reclassified here (X).

**External (standard causal-set theory; invoked, not re-derived).**
- L. Bombelli, J. Lee, D. Meyer, R. D. Sorkin, *Space-time as a causal set*, Phys. Rev. Lett. **59**, 521 (1987) — order + number = geometry.
- D. Malament, *The class of continuous timelike curves determines the topology of spacetime*, J. Math. Phys. **18**, 1399 (1977); S. W. Hawking, A. R. King, P. J. McCarthy, J. Math. Phys. **17**, 174 (1976) — order determines the conformal structure of a manifold-like spacetime.
- J. Myrheim, CERN preprint TH-2538 (1978); D. Meyer, *The dimension of causal sets* (PhD thesis, MIT, 1988) — the dimension-from-order estimator.
- D. Kleitman, B. Rothschild, *Asymptotic enumeration of partial orders on a finite set*, Trans. Amer. Math. Soc. **205**, 205 (1975) — the dominance of 3-layer non-manifold orders.
- L. Bombelli, J. Noldus, *The moduli space of isometry classes of globally hyperbolic spacetimes* (2004) — the causal-set Hauptvermutung context.
- D. P. Rideout, R. D. Sorkin, *Classical sequential growth dynamics for causal sets*, Phys. Rev. D **61**, 024002 (2000) — causal-set dynamics, the arena for the manifoldlikeness-selection question.
