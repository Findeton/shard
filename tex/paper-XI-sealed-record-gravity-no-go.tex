\documentclass[11pt]{article}
\usepackage{fontspec}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage[margin=1.1in]{geometry}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{lmodern}
\usepackage{xcolor}
\usepackage{adjustbox}
\usepackage[colorlinks=true,linkcolor=blue!50!black,urlcolor=blue!50!black]{hyperref}
\providecommand{\tightlist}{}
\setlength{\emergencystretch}{3.5em}
\title{Emergent Einstein equations without an emergent Newton constant: a no-go for sealed-record gravity}
\author{Felix Robles Elvira (ORCID: 0009-0009-2017-4394; independent researcher)}
\date{\small preprint, not peer reviewed, version 2026-06-14. Every load-bearing line is tagged by epistemic status: \textbf{[DERIVED]} = established (symbolic or high-precision numeric), with its conditional gates named; \textbf{[NO-GO]} = a proved obstruction; \textbf{[OBSTRUCTED]} = blocked in practice, with the obstruction characterized; \textbf{[OPEN]} = an unsolved obligation. All numeric claims were verified at mpmath \texttt{dps ≥ 80} (commonly 100) and symbolically (sympy) where exact; the receipt script is \texttt{code/v6\_pXI\_sealed\_record\_gravity\_nogo\_receipts.py}.}
\begin{document}
\maketitle

\section*{Abstract}
Within the sealed-records program — physics as whole histories of committed records rather than instantaneous states — we give the gravity sector its sharp, two-sided verdict. \textbf{The records derive gravity's equation of state, and prove they cannot fix its scale.} On the positive side, taking the horizon temperature \texttt{T = a/2π} as established elsewhere in this batch (Euclidean-cone regularity and Bisognano–Wichmann modular structure), we show the horizon record entropy is structurally \textbf{pure-area} — a curvature-free Wald–Noether charge of the Einstein sector (\texttt{dσ\_A/dR = 0} by construction) — so that the Clausius relation \texttt{δQ = TδS} and the modular first law \texttt{δS = δ⟨K⟩} close, yielding the Einstein field equations \emph{in form}. The derivation reaches the \textbf{full nonlinear} equation \texttt{G\_μν + Λg\_μν = 8πG T\_μν} (here \texttt{Λ} is the cosmological constant; the no-go invariant \texttt{G·Λ²} below instead uses \texttt{Λ} for the inverse-length UV cutoff — a distinct object) — the pointwise null relation \texttt{2π T\_kk = η R\_kk} for the actual Ricci tensor, checked nonlinearly — and its \textbf{spin-2 (traceless)} part follows as a one-step geometric identity: a symmetric tensor annihilating every null contraction is pure-trace (\texttt{S\_ab k\textasciicircum{}a k\textasciicircum{}b = 0 ∀} null \texttt{k ⇒ S\_ab = Φ g\_ab}, the constraint having rank \texttt{9} on the \texttt{10} symmetric components). This is [DERIVED], modulo three named gates — the temperature value (axiom R), Jacobson's local-equilibrium premise \texttt{θ = σ = 0}, and one continuum focusing input. On the negative side we prove a \textbf{no-go}: the records cannot fix the absolute scale \texttt{σ\_A} (length-weight \texttt{−2}, in bijection with Newton's \texttt{G} via \texttt{G·σ\_A = 1/4}). Under the length-unit gauge \texttt{l → μl}, every intrinsic record functional is weight-zero while \texttt{σ\_A} is weight \texttt{−2}; the contradiction is exact (weight-counting lemma), and the SIGMA-SPLIT construction makes it concrete — the \emph{same} sealed horizon data are consistent with \texttt{A\_rec = 1} or \texttt{A\_rec = 3}, giving the schematic Clausius coefficient \texttt{κ\_J ≡ 1/σ\_A = 1} or \texttt{3} with the Einstein residual \texttt{0} for both. Both \texttt{κ·σ\_A = 2π} (the physical coupling \texttt{κ = 8πG}) and \texttt{G·Λ²} (with \texttt{Λ} the inverse-length UV cutoff, distinct from the unimodular \texttt{Λ₀}) are fixed weight-zero invariants (separate pure numbers, not numerically equal), each leaving the absolute scale free. The entire no-go rests on one isolated premise — \textbf{gate G1}: no sealed law consumes the record area outside the labeling map — exactly as the temperature value rests on axiom R; its sharpest candidate counterexample, an intrinsic seal rate, closes rather than opens it. We show the obstruction is \textbf{the same structural fact as QCD's dimensional transmutation} (a scale-free record ontology predicts every dimensionless ratio and needs one external unit), and we separate the \emph{derived} spin-2 equation from the \emph{obstructed} propagating graviton, which would require a continuum (type III₁) structure the finite record lattice cannot host. The result places sealed-record gravity squarely in the emergent/thermodynamic tradition: \textbf{gravity's form for free, its scale and its graviton provably not.}

---

\section*{1. What is derived: the equation of state}
\subsection*{1.1 Temperature [DERIVED, mod axiom R — established in the horizon-thermodynamics paper of this batch]}
The thermodynamic derivation begins with a temperature. The horizon-thermodynamics paper of this batch establishes it two ways on record lattices: the Euclidean cone is smooth at the unique period \texttt{β = 2π/κ} (Gibbons–Hawking regularity, with Bessel-mode matching), and the wedge-reduced record vacuum exhibits Bisognano–Wichmann modular structure \texttt{ε\_k = 2π ω\_k\textasciicircum{}{boost}} uniformly across resolvable modular levels. We take \texttt{T = a/2π} as input. Its value \texttt{β = 2π} rests on one named axiom (R) — the Euclidean-rotation / modular-period identification, the residue of Bisognano–Wichmann — and we carry that conditional forward without re-deriving it here. As a high-precision consistency check, the accelerated KMS detailed-balance ratio \texttt{F(E)/F(−E) = e\textasciicircum{}{−2πE/a}} holds identically at \texttt{T = a/2π} (residual \texttt{0} at \texttt{dps = 100}), and the Rindler interval identity \texttt{Δt² − Δx² = (4/a²)\textbackslash{}sinh²(aΔτ/2)} is exact.

\subsection*{1.2 Geometry-universal area density [DERIVED]}
The pure-geometry content of the area law is geometry-universal. A \texttt{3+1} massless scalar partial-wave-decomposes into radial chains, and the ball entropy \texttt{S(R) = Σ\_l (2l+1) S\_l(R)} gives an area coefficient that Richardson-extrapolates to \textbf{Srednicki's \texttt{0.295}} [5] with no fitted tail — a genuine universal constant (the spherical-ball partial-wave computation is the program's entanglement-capacity calculation). The ball/planar capacity ratio is \texttt{U\_G = 1} to \texttt{\textasciitilde{}10⁻³}. Crucially, the absolute scale \texttt{σ\_A} \emph{cancels} in this ratio (\texttt{ν = σ\_A·f\_geom}, \texttt{U\_G = f\_sph/f\_pl}), so the \emph{geometry factor} \texttt{f\_geom} is record-derived while the overall scale is not. Two caveats are kept honest: \texttt{U\_G = 1} \emph{straddles} unity at the \texttt{\textasciitilde{}10⁻³} level (the sign of \texttt{U\_G − 1} is cutoff-limited, a consistency not a sign-determination), and flat-screen universality is \emph{measured to break in the angular sector} (the partial-wave first-law ratio departs from unity for \texttt{l ≥ 1}). So [DERIVED] covers the area-coefficient ratio and the scalar (\texttt{l = 0}) channel; the angular sector is a measured departure.

\subsection*{1.3 Pure-area Wald–Noether charge [DERIVED, structural]}
The record entropy is screen-cell counting, \texttt{S = σ\_A·A + o(A)}, with curvature pieces explicitly subleading. The Wald density is then \texttt{s = σ\_A} with \texttt{ds/dR = 0} \textbf{by construction} — the screen-cell capacity does not couple to a curvature scalar — so this is the \emph{statement} that \texttt{σ\_A} is curvature-free, a structural property of the screen-cell entropy ontology corroborated by the floor-converged mode-cutoff sweep (the apparent curvature-running shrinks monotonically with the cutoff, consistent with a truncation artifact rather than a Wald coupling). This is \textbf{not} a derivation that an arbitrary higher-curvature gravitational action generates no Wald corrections; it is the statement that \emph{this} ontology's entropy is pure-area. The consequence is that \texttt{η = 1/4G} is well-defined (not running), keeping the Clausius route in the equilibrium (pure-Einstein) branch rather than the nonequilibrium \texttt{f(R)}/Lovelock branch.

\subsection*{1.4 The field equations in form [DERIVED, mod R + local-equilibrium + focusing gate]}
With a pure-area entropy at temperature \texttt{T = a/2π}, two closures deliver the Einstein equation in form.

\textbf{Clausius (nonlinear).} The Jacobson construction on local Rindler horizons is exact: Raychaudhuri gives \texttt{θ = −R\_{kk}λ + O(λ²)}, the heat- and area-side affine moments cancel, and the pointwise null relation \texttt{2π T\_{kk} = η R\_{kk}} holds for the \textbf{actual Ricci tensor} — checked nonlinearly on explicit Schwarzschild and on an anisotropic fluid with genuine traceless stress (corpus computations), with contracted Bianchi closing. The coefficient match \texttt{2π/η = 8πG} forces \texttt{η = 1/4G}. This is the \textbf{full nonlinear} equation \texttt{G\_μν + Λg\_μν = 8πG T\_μν}, not a linearized truncation.

\textbf{Modular first law (linearized) and the spin-2 part.} The relative-entropy first law \texttt{δS = δ⟨K⟩} (with \texttt{S\_rel ≥ 0} strictly second order) yields the linearized equation. Its traceless (spin-2) part follows as a one-step geometric identity: the null-cone lemma — \emph{a symmetric tensor \texttt{S\_ab} with \texttt{S\_ab k\textasciicircum{}a k\textasciicircum{}b = 0} for every null \texttt{k} is pure-trace, \texttt{S\_ab = Φ g\_ab}} — fixes \textbf{all 9 traceless components at once} (the constraint over a spanning set of null \texttt{k} has rank \texttt{9} on the \texttt{10} symmetric components; sympy). The same geometric content underlies Jacobson's entanglement-equilibrium derivation; the correct timelike statement of it is that a symmetric \texttt{E\_ab} whose contraction \texttt{E\_ab u\textasciicircum{}a u\textasciicircum{}b} is the \emph{same} for every unit timelike \texttt{u} is \texttt{c g\_ab} (the naive vanishing condition \texttt{E\_ab u\textasciicircum{}a u\textasciicircum{}b = 0 ∀} unit timelike \texttt{u} instead forces \texttt{E = 0}, and is \emph{not} the lemma). The traceless projection removes the free proportionality coefficient \texttt{c} (the cutoff scale \texttt{Λ}), so the \textbf{shape} of the spin-2 equation is scale-free; \texttt{G} itself remains explicitly, as the coupling on the right-hand side \texttt{8πG T\_{⟨ab⟩}}.

\textbf{Conditionality.} Three named gates carry the form: axiom (R) (§1.1); Jacobson's \textbf{local-equilibrium premise} \texttt{θ = σ = 0} at the bifurcation — \emph{presumed, not derived} — which selects pure Einstein over the nonequilibrium branch; and the continuum focusing input \texttt{θ′ = −R\_{kk}}. None of these bears on the no-go of §2.

\subsection*{1.5 The unimodular fork [DERIVED]}
Trace-free Einstein \texttt{R\_ab − ¼g\_ab R = 8πG(T\_ab − ¼g\_ab T)} is a genuine 9-component equation in \texttt{d = 4}; its divergence plus contracted Bianchi gives, with \texttt{Λ := (R + 8πG T)/4}, exactly \texttt{∇\_b Λ = 8πG η\_b} (residual identically \texttt{0}, sympy) — the conservation/Bianchi obstruction \emph{rerouted} into a dynamical \texttt{Λ}, not evaded, with the \emph{same} \texttt{8πG} (no second coupling). The cosmological constant becomes a non-sourced integration constant \texttt{Λ₀}, a boundary datum.

So sealed-record gravity reaches the Einstein field equations \textbf{as the thermodynamics of the records}, deriving more than the vanilla thermodynamic program assumes (the geometry-universality and the pure-area property are derived here, not posited). The remainder establishes precisely where, and why, the path stops.

---

\section*{2. The no-go on Newton's constant [NO-GO]}
\subsection*{2.1 The result and its single premise}
The records do not, and provably cannot, fix the absolute scale \texttt{σ\_A} — which has length-weight \texttt{−2} and is in bijection with Newton's \texttt{G} (weight \texttt{+2}) via \texttt{G·σ\_A = 1/4} (so fixing one fixes the other; they are linked, never equal). Under the length-unit gauge \texttt{g\_λ: l → μl} holding all sealed data fixed, the records carry only weight-zero (dimensionless / ratio / conformal) invariants. The degree map \texttt{weight(X) = \textbackslash{}log[X(μl)/X(l)]/\textbackslash{}log μ} gives, sympy-exact (and numerically at \texttt{μ = 1.7}, residual \texttt{0}):

\texttt{weight(A\_rec = l²) = +2},  \texttt{weight(σ\_A = 1/l²) = −2},  \texttt{weight(G) = +2},  \texttt{weight(l\_step) = +1},  \texttt{weight(\textbackslash{}text{pure count}) = 0},

with the bijection \texttt{G·σ\_A} of weight \texttt{0} and value \texttt{1/4}. Both

\texttt{κ·σ\_A = 2π}   and (separately)   \texttt{G·Λ² = \textbackslash{}text{const}}

are fixed weight-zero invariants — \emph{each} a pure number, \textbf{not numerically equal to one another} (\texttt{κ·σ\_A = 2π} follows from \texttt{κ = 8πG} and \texttt{G·σ\_A = 1/4}; \texttt{G·Λ²} is a different pure number set by whichever route computes it). What they share is the \emph{structure}: a weight-zero combination is fixed while the absolute scale \texttt{σ\_A}/\texttt{G} is a free modulus. (Two normalizations of the Clausius coefficient appear: the physical \texttt{κ = 8πG} in \texttt{κ·σ\_A = 2π}, and the schematic \texttt{κ\_J ≡ 1/σ\_A} in the SIGMA-SPLIT of §2.2, where \texttt{κ\_J·σ\_A = 1}. Throughout, \texttt{Λ} is the inverse-length UV/spectral cutoff, distinct from the unimodular cosmological constant \texttt{Λ₀} of §1.5.)

\textbf{The single load-bearing premise — gate G1.} The no-go rests on exactly one premise, isolated as cleanly as axiom (R) isolates the temperature value: \textbf{gate G1} — \emph{no sealed law consumes the record area \texttt{A\_rec} (a weight-\texttt{±k} dimensionful datum) except through the continuum labeling map \texttt{ℓ}.} G1 is what makes the corpus factor as \texttt{(R, ℓ)} with \texttt{g\_λ} the identity on the record sector \texttt{R}, so that every intrinsic record functional factors through \texttt{R}, is \texttt{g\_λ}-invariant, and is therefore weight-zero — while \texttt{σ\_A} is weight \texttt{−2}. The no-go is airtight \emph{iff} G1 holds. Its sharpest candidate counterexample, an intrinsic \emph{seal rate} \texttt{Γ} (divisions per proper time), \textbf{closes} rather than opens it: a record-internal \texttt{Γ} is divisions-per-seal \texttt{= 1} (weight 0, no scale); any geometric \texttt{Γ} is the seal density \texttt{1/l\_step} (weight \texttt{−1}, the gauge/spacing direction itself); and a putative weight-\texttt{(−2)} \texttt{Γ² = (\#²)/A\_rec} is a free record number times \texttt{1/A\_rec} (a relabeling, never the absolute \texttt{σ\_A}) — an intrinsic absolute scale would need weight \texttt{−2} \emph{and} weight \texttt{0} at once, which the factorization forbids. So the seal-spacing length is provably the unique dimensionful primitive and the sole gauge direction.

\subsection*{2.2 SIGMA-SPLIT: the gauge orbit made concrete [NO-GO]}
The mechanism is visible in one construction. The \emph{same} sealed horizon data are consistent with two screen-cell normalizations, \texttt{A\_rec = 1} or \texttt{A\_rec = 3}, giving

\texttt{A\_rec = 1 ⟹ σ\_A = 1, κ\_J = 1, G = 1/4};  \texttt{A\_rec = 3 ⟹ σ\_A = 1/3, κ\_J = 3, G = 3/4},

with \texttt{κ\_J ≡ 1/σ\_A} the schematic Clausius coefficient, \texttt{G·σ\_A = 1/4} invariant in both, and the Einstein-form residual exactly \texttt{0} in both (sympy). The absolute \texttt{G} is free; only the gauge-invariant product is fixed. A scale-rescaling check confirms it: \texttt{l → μl} sends \texttt{G → μ²G} while \emph{every} sealed, order, and modular observable is invariant.

\subsection*{2.3 Every lever collapses the same way [NO-GO]}
Each candidate route to fix \texttt{G} reduces to the same weight-zero invariant. A mass-gap correlation length is \texttt{(\textbackslash{}\#)·l\_step} (lattice units, not an independent length); a second marginal sector transmutes a \emph{dimensionless} fixed-point coupling, tying sectors to one another, never to a geometric length; an asymptotic-safety fixed point \texttt{g\_* = Gμ²} gives \texttt{G = g\_* l\_step²}, reproducing SIGMA-SPLIT; the conformal trace anomaly \texttt{(a, c)} fixes dimensionless higher-curvature couplings only; Sakharov-induced gravity gives \texttt{G·Λ² = 192π²/N} with \texttt{Λ} cancelling. The \textbf{de Sitter} route is the sharpest test and still collapses: the unimodular \texttt{Λ₀} is itself weight \texttt{−2} (the weight-twin of \texttt{σ\_A}), and the entropy \texttt{S\_dS = π/(G·Λ₀) = N\_dS} (prefactor schematic — the textbook value is \texttt{3π/(GΛ₀)}; the weight argument is prefactor-independent) fixes only the weight-zero product \texttt{G·Λ₀}, with \texttt{N\_dS = A\_horizon/A\_rec = 4π(σ\_A/Λ₀)} the ratio of two weight-\texttt{(−2)} data — \texttt{l}-independent (sympy \texttt{d/dl = 0}), so it adds no gauge-eligible datum and \texttt{G} stays free. Even the strongest \emph{record-unit} attack fails: measuring a mass in record units smuggles in the record-length↔lab-length conversion that \texttt{g\_λ} acts on. The unification is the content: \emph{deriving \texttt{1/G} always trades it for a labeling-equivalent cutoff; the missing datum is exactly one absolute length unit, which no weight-zero record functional can be.}

\subsection*{2.4 The same fact as dimensional transmutation}
The no-go is not a pathology; it is the gravity-sector face of a familiar fact. A confining gauge theory predicts every dimensionless mass \emph{ratio} and the running of a dimensionless coupling, but \textbf{dimensional transmutation does not produce an absolute scale} — \texttt{Λ\_QCD} in physical units is an external input, and a lattice mass gap comes out as a pure number times the lattice spacing. Sealed-record gravity says the same of \texttt{G}: the records fix every dimensionless ratio and the Einstein \emph{form}, and need one external unit to set the scale. What the no-go forbids is \emph{one absolute dimensionful unit} — \textbf{not} the dimensionless gravitational coupling-per-species \texttt{c\_m = Gm²/ℏc}, which is weight-zero, intrinsic, and therefore \emph{eligible} to be a record output (the open hierarchy question, the gravity-sector analog of predicting dimensionless mass ratios). A theory that reconstructed gauge physics up to one scale yet fixed \texttt{G} absolutely would be the contradiction; the no-go is the program being consistent with itself.

---

\section*{3. The tensor sector: the spin-2 equation is derived, the graviton is not}
The linearized spin-2 Einstein \emph{equation} \texttt{G\_{⟨ab⟩} = 8πG T\_{⟨ab⟩}} is derived geometrically (§1.4). What is \textbf{[OBSTRUCTED]} is the \emph{distinct} claim that the matter modular \emph{currency} independently \textbf{prices} the traceless stress \texttt{T\_{⟨ij⟩}} as a universal first-law charge — i.e. the propagating graviton / quantum spin-2 operator, \emph{not} the equation. The modular currency prices the 4 energy–momentum components (\texttt{T\_{00}}, \texttt{T\_{0i}}) at a universal capacity \texttt{1/(4ν)}; a congruence of boosted wedges does \textbf{span} the 5 traceless spatial-stress components (rank 5), but only at a \textbf{non-universal, boost-dependent, second-order} coupling \texttt{χ·\textbackslash{}sinh²η}, never the single universal capacity that prices energy and momentum.

Four obstructions converge on \textbf{one root}. (i) \emph{Non-universality}: the traceless coupling is a second-order susceptibility, not a capacity. (ii) \emph{Type I, no area operator}: the finite record lattice is a type-I algebra with no center-valued area operator; the crossed-product constructions that manufacture a fluctuating area operator from an algebra lacking one are blocked — the modular flow is inner/trivial, and the entropy-production (arrow) flow, though genuinely outer, is a \emph{stationary current} preserving an invariant measure, so its crossed product is semifinite (type I, c-number area), not the type III₁ needed. (iii) \emph{Berry}: the first-order relative modular charge between boosted wedges has zero spin-2 (\texttt{\textbackslash{}cos 2θ}) overlap. (iv) \emph{Null-cut}: the universal-\texttt{2π} null-cut pricing \emph{is} a half-sided modular inclusion \texttt{[K,P] = iP}, which has no finite-dimensional representation. The single root: \textbf{every spin-2 / quantum-area route needs an outer modular flow, a type III₁ algebra, and a half-sided inclusion — structures that live only in the continuum local algebra, which the finite record lattice cannot host intrinsically.} So sealed-record geometry is spin-2-\emph{active} but not-a-graviton; a genuine propagating spin-2 (the real-time transverse-traceless sector, an emergent holographic bulk) is gated and remains [OPEN].

---

\section*{4. The open frontiers, and the emergent/intrinsic distinction}
Two frontiers remain open; neither bears on the no-go of §2.

\textbf{Covariance of the dynamics. [OBSTRUCTED-leaning]} Done the causal-set way, discreteness covariantizes the \emph{kinematics} outright: a Poisson sprinkling of division events is statistically Lorentz-invariant; the entropy-production arrow \texttt{σ = D(P\_AB‖P\_BA)} reads the frame-invariant causal partial order, not a preferred slice; and the free-flash localization dissolves Hegerfeldt's obstruction (a point-event sampled from a density, not a positive-energy state). The interacting-\emph{distinguishable} dynamics is solved (Tumulka 2020 [12]). The genuine residue is a covariant interacting-\emph{field} (variable particle number) record dynamics, which remains [OBSTRUCTED-leaning].

\textbf{The propagating graviton and the real-time dynamics. [OPEN]} Beyond the static modular-currency program lies the real-time transverse-traceless sector — the radiative, propagating content of gravity, whose quantum is the graviton. The split is sharp: an \emph{intrinsic finite-record} graviton is obstructed (§3, the type-I/type-III₁ wall), while an \emph{emergent continuum} graviton — gravity as an effective field theory of the coarse-grained metric, reached in a scaling limit — is open, not obstructed, and is the same continuum-limit program as reconstructing continuum field theory from the finite lattice. So the records carry the \emph{equation of state} of gravity, and the \emph{dynamics} of gravity (its radiative, propagating sector) is the emergent-continuum-limit frontier, not an intrinsic finite-record object.

The observability counterpart of these frontiers — whether the gravitational record process is genuinely indivisible, and whether that is measurable — is treated in the companion decoherence paper of this batch, which proves it is structural and not certifiable from the reduced channel.

---

\section*{5. Status ledger}
\begin{center}
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{lll}
Object & Status & Statement \\
\hline
Temperature \texttt{T = a/2π} & \textbf{[DERIVED, mod R]} & established in the horizon paper; value rests on axiom (R) \\
Geometry factor \texttt{f\_geom} (area coeff. + \texttt{l=0}) & \textbf{[DERIVED]} & \texttt{U\_G = 1} to \texttt{\textasciitilde{}10⁻³} (straddles); Srednicki \texttt{0.295}, no fit \\
Angular first-law universality & \textbf{[OPEN]} & departs for \texttt{l ≥ 1}; measured, not closed \\
Horizon entropy = pure-area Wald charge & \textbf{[DERIVED, structural]} & \texttt{ds/dR = 0} by construction; corroborated by the floor-converged sweep \\
Clausius / modular first law → Einstein \emph{form} & \textbf{[DERIVED, mod R + θ=σ=0 + focusing]} & full nonlinear via Clausius; linearized via \texttt{δS = δ⟨K⟩}; \texttt{η = 1/4G} \\
Cosmological constant \texttt{Λ₀} (unimodular) & \textbf{[DERIVED]} & non-sourced integration constant; Bianchi rerouted \\
Absolute scale \texttt{σ\_A} ↔ \texttt{G} & \textbf{[NO-GO]} & \texttt{κ·σ\_A} and \texttt{G·Λ²} fixed weight-zero invariants, scale free; weight-counting lemma; SIGMA-SPLIT; single premise gate G1 (seal-rate attack closes it); at audited scope \\
Linearized spin-2 (traceless) Einstein \emph{equation} & \textbf{[DERIVED, mod R + gates]} & null-cone lemma fixes all 9 traceless comps; traceless \emph{shape} scale-free (\texttt{G} remains on the RHS) \\
Spin-2 matter \emph{charge} / propagating graviton & \textbf{[OBSTRUCTED → OPEN]} & non-universal + type-I + Berry + half-sided-inclusion; real-time TT sector gated \\
Covariant interacting \emph{field} dynamics & \textbf{[OBSTRUCTED-leaning]} & kinematics covariantized; interacting-distinguishable solved; field case open \\
\end{tabular}
\end{adjustbox}
\end{center}
---

\section*{6. What this paper does and does not claim}
It \textbf{claims}: a derivation, from the sealed-record ontology, of the gravitational field equations as an equation of state (temperature + pure-area geometry-universal entropy + modular/Clausius first law), including the full nonlinear form and the linearized spin-2 (traceless) equation as a geometric identity, modulo the three named gates; and a no-go (at audited corpus scope, with gate G1 as its single load-bearing premise) that the absolute coupling \texttt{G} is not encoded in the records (\texttt{κ·σ\_A} and \texttt{G·Λ²} weight-zero invariants; the weight-counting lemma; SIGMA-SPLIT).

It does \textbf{not} claim: a derivation of Newton's \texttt{G} (proved impossible, modulo the audit scope); a propagating graviton / quantum spin-2 charge (obstructed — the type-I/type-III₁ wall — and distinct from the derived equation); the real-time/radiative dynamics of gravity (open, the emergent-continuum frontier); a covariant interacting-field dynamics (open, with the kinematics covariantized); or the matter-sector hierarchy \texttt{c\_m} (a separate, intrinsic, still-open dimensionless quantity the no-go does \emph{not} forbid). The framing is thermodynamic: gravity as the equation of state of records, with the entropy-area density a calibration — and the theorem (at audited scope) that the missing datum is exactly \emph{one absolute unit}. The honest one-line summary: \textbf{the records give gravity's form for free and prove they cannot give gravity's scale.}

---

\section*{7. Precision discipline}
Every modular-kernel, near-vacuum, and weight-bookkeeping quantity was computed in mpmath at \texttt{dps ≥ 80} (commonly 100), with exact algebra (the weight degree map, SIGMA-SPLIT, the null-cone lemma, the trace-free / Bianchi identities, the de Sitter \texttt{l}-cancellation, the KMS detailed-balance and Rindler identities) in sympy. The receipt script \texttt{code/v6\_pXI\_sealed\_record\_gravity\_nogo\_receipts.py} reproduces the no-go and equation-of-state numbers below (the §1.2 geometry-universality constants — Srednicki's \texttt{0.295} [5] and the \texttt{U\_G} ratio — are the corpus's spherical-ball partial-wave computation, separate from this receipt): the weight map (residuals \texttt{0}), the bijection \texttt{G·σ\_A = 1/4} (weight \texttt{0}), SIGMA-SPLIT (\texttt{κ = 1} vs \texttt{3}, residual \texttt{0}), the two separate invariants \texttt{κ·σ\_A = 2π} and \texttt{G·Λ²}, the de Sitter weight-twin (\texttt{N\_dS} \texttt{l}-independent), the null-cone lemma (rank 9; pure-trace solution) together with the correct-versus-naive timelike statement, the spectral-action honesty check (the naive collar profile gives \texttt{f₂ = 1/(2π)² = 0.025330…}, \emph{not} \texttt{1/(2π)}, so \texttt{G·Λ² = 3π²} does not follow), and the KMS/Rindler identities. Never float64 for the modular-kernel or near-vacuum quantities, where the corpus audit shows a 25–32\% artifact.

---

\section*{References}
\textbf{Companion papers.}

[V] \emph{Quantum theory from sealed records I–II}, companion papers (\texttt{paper-Va-foundations-1}, \texttt{paper-Vb-foundations-2}) — axioms R, S, C; the \texttt{q = 2} weight calculus; Lorentz signature from commitment order. [VIII] \emph{Horizon thermodynamics on record lattices}, companion paper (\texttt{paper-VIII-horizons}) — temperature two ways (Euclidean cone \texttt{β = 2π/κ} and Bisognano–Wichmann modular structure), censorship, greybody impedance, capacity; the input for §1.1. [X] \emph{Gravitational decoherence does not certify its mechanism}, companion paper (\texttt{paper-X-gravitational-decoherence}) — the operational-undecidability counterpart of the open covariance/indivisibility frontier of §4.

\textbf{External.}

[1] T. Jacobson, \emph{Thermodynamics of spacetime: the Einstein equation of state}, Phys. Rev. Lett. \textbf{75}, 1260 (1995). [2] T. Jacobson, \emph{Entanglement equilibrium and the Einstein equation}, Phys. Rev. Lett. \textbf{116}, 201101 (2016). [3] C. Eling, R. Guedens, T. Jacobson, \emph{Nonequilibrium thermodynamics of spacetime}, Phys. Rev. Lett. \textbf{96}, 121301 (2006). [4] R. M. Wald, \emph{Black hole entropy is the Noether charge}, Phys. Rev. D \textbf{48}, R3427 (1993). [5] M. Srednicki, \emph{Entropy and area}, Phys. Rev. Lett. \textbf{71}, 666 (1993). [6] A. D. Sakharov, \emph{Vacuum quantum fluctuations in curved space and the theory of gravitation}, Sov. Phys. Dokl. \textbf{12}, 1040 (1968). [7] T. Padmanabhan, \emph{Thermodynamical aspects of gravity: new insights}, Rep. Prog. Phys. \textbf{73}, 046901 (2010). [8] E. Verlinde, \emph{On the origin of gravity and the laws of Newton}, JHEP \textbf{04}, 029 (2011). [9] H.-J. Borchers, \emph{On revolutionizing quantum field theory with Tomita's modular theory}, J. Math. Phys. \textbf{41}, 3604 (2000). [10] V. Chandrasekaran, R. Longo, G. Penington, E. Witten, \emph{An algebra of observables for de Sitter space}, JHEP \textbf{02}, 082 (2023). [11] N. Lashkari, J. Lin, H. Marrochio, R. Myers, R. C. Myers — \emph{modular/relative-entropy first law and gravitational dynamics} (entanglement-equilibrium and JLMS lineage). [12] R. Tumulka, \emph{A relativistic version of the GRW flash model}, J. Stat. Phys. \textbf{125}, 825 (2006); and the interacting-distinguishable extension (2020).

\end{document}
